app:
  extends:
    file: common.yml
    service: app
  links:
    - db
  ports:
    - 8000:8000
  command: uwsgi --http :8000 --module mysite.wsgi

db:
  extends:
    file: common.yml
    service: db

agent:
  extends:
    file: common.yml
    service: agent
  links:
    - db